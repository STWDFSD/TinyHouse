import * as BABYLON from "@babylonjs/core";

export function test(scene, engine) {
  // const xMesh = BABYLON.Mesh.CreateBox("xBoxMesh", 1, scene);
  // xMesh.position = new BABYLON.Vector3(10, 0, 0);
  // const yMesh = BABYLON.Mesh.CreateBox("yBoxMesh", 2, scene);
  // yMesh.position = new BABYLON.Vector3(0, 10, 0);
  // const pbr = new BABYLON.PBRMetallicRoughnessMaterial("pbr", scene);
  // yMesh.material = pbr;
  // pbr.baseColor = new BABYLON.Color3(1.0, 0, 1.0);
  // pbr.metallic = 1.0;
  // pbr.roughness = 0;
  // pbr.environmentTexture = BABYLON.CubeTexture.CreateFromPrefilteredData(
  //   "/Textures/DDS/environment.dds",
  //   scene
  // );
  // const zMesh = BABYLON.Mesh.CreateBox("zBoxMesh", 3, scene);
  // zMesh.position = new BABYLON.Vector3(0, 0, 10);
}
